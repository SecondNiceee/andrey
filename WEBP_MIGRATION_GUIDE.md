# 🚀 Полная Миграция PNG → WebP

Это руководство по полной миграции всех PNG изображений на WebP с удалением оригиналов и обновлением всех HTML файлов.

## 📋 Что происходит

1. **Конвертация** - все PNG/JPG файлы конвертируются в WebP (качество 70%)
2. **Обновление HTML** - все ссылки в HTML файлах обновляются на WebP версии
3. **Удаление** - старые PNG файлы удаляются из проекта

## 🚀 Быстрый Старт (Рекомендуется)

### Вариант 1: Полная автоматическая миграция (ВСЕ СРАЗУ)

```bash
npm run migrate-to-webp
```

Этот скрипт запустит все три этапа по порядку:
1. Конвертация PNG → WebP
2. Обновление HTML файлов
3. Удаление старых PNG

## 📚 Пошаговый Запуск (Если нужен контроль)

Если ты хочешь запустить скрипты отдельно:

### Шаг 1: Конвертация PNG в WebP

```bash
npm run convert-all-webp
```

Это создаст:
- `img-no/` → WebP версии всех файлов
- `img/` → WebP версии всех файлов
- `scripts/processed-images.json` → список обработанных файлов

**Результат:**
```
✅ img/bottle1.png → bottle1.webp
✅ img/logo.png → logo.webp
...
```

### Шаг 2: Обновить HTML файлы

```bash
npm run update-html-comprehensive
```

Это обновит все `.html` файлы в проекте:
```
✅ index.html: заменено 120 ссылок
✅ catalog.html: заменено 45 ссылок
```

Скрипт заменит:
- `./img/logo.png` → `./img/logo.webp`
- `img/bottle1.png` → `img/bottle1.webp`
- И все варианты путей

### Шаг 3: Удалить старые PNG файлы

```bash
npm run delete-png
```

Удалит все старые PNG файлы:
```
✅ Удален: img/bottle1.png
✅ Удален: img/logo.png
...
✅ Успешно удалено: 150 файлов
```

## 📊 Структура Скриптов

| Скрипт | Команда | Назначение |
|--------|---------|-----------|
| `convert-all-to-webp.js` | `npm run convert-all-webp` | Конвертирует PNG → WebP |
| `update-html-comprehensive.js` | `npm run update-html-comprehensive` | Обновляет все HTML файлы |
| `delete-old-png.js` | `npm run delete-png` | Удаляет старые PNG |
| `full-migration-to-webp.js` | `npm run migrate-to-webp` | Запускает все три шага |

## 🔍 Что Изменится

**ДО (PNG):**
```html
<img src="./img/logo.png" alt="Logo" />
<img src="img/bottle1.png" class="bottle" />
```

**ПОСЛЕ (WebP):**
```html
<img src="./img/logo.webp" alt="Logo" />
<img src="img/bottle1.webp" class="bottle" />
```

**Файловая система:**
```
img/
├── logo.webp          ✅ (новое)
├── bottle1.webp       ✅ (новое)
├── logo.png           ❌ (удалено)
└── bottle1.png        ❌ (удалено)
```

## 🎯 Результаты

- **Размер:** WebP обычно на 25-35% меньше, чем PNG при том же качестве
- **Качество:** 70% качество сохраняет нормальный вид при меньшем размере
- **Совместимость:** Все современные браузеры поддерживают WebP

## ⚠️ Важно

1. **Бэкап:** Перед запуском убедись что у тебя есть бэкап проекта в Git
2. **Проверка:** После миграции проверь сайт - все ли изображения отображаются
3. **PNG ссылки:** После удаления старые ссылки на `.png` больше не будут работать

## 🔧 Если Что-то Пошло Не Так

### Если скрипт упал на шаге конвертации:
```bash
npm run convert-all-webp
```

### Если ссылки в HTML не обновились:
```bash
npm run update-html-comprehensive
```

### Если нужно вернуть PNG:
У тебя всегда есть версия в Git, используй `git restore` или вернись к предыдущему коммиту.

## 📈 Проверка Результатов

После завершения миграции проверь:

1. **Файлы WebP созданы:**
   ```bash
   ls -la img/*.webp | wc -l  # Должны быть WebP файлы
   ```

2. **HTML файлы обновлены:**
   ```bash
   grep -c "\.webp" index.html  # Должны быть ссылки на WebP
   ```

3. **PNG файлы удалены:**
   ```bash
   ls -la img/*.png 2>/dev/null | wc -l  # Должно быть 0
   ```

## 💡 Советы

- Качество 70% - это оптимум между размером и качеством
- Если нужно более высокое качество, отредактируй `convert-all-to-webp.js` - замени `quality: 70` на `quality: 80` или `quality: 90`
- Скрипты создают `processed-images.json` - это список всех конвертированных файлов для отслеживания

## 🆘 Поддержка

Если у тебя есть проблемы:
1. Проверь что `sharp` установлен: `npm list sharp`
2. Проверь что файл `processed-images.json` существует после конвертации
3. Проверь логи скрипта для подробной информации об ошибках
