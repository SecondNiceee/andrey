# 🎓 Примеры и FAQ

## 📚 Примеры Использования

### Пример 1: Полная Миграция (Рекомендуется)

```bash
# Запускаем все три этапа подряд
npm run migrate-to-webp

# Вывод:
# ============================================================
# 📊 Запускаю: Конвертация PNG → WebP
# ============================================================
# 🚀 Начинаю конвертацию ВСЕХ изображений в WebP...
# ✅ img/bottle1.png → bottle1.webp
# ✅ img/bottle2.png → bottle2.webp
# ... 150+ файлов
# ✨ Конвертация завершена!
# ✅ Успешно обработано: 150
# 
# ============================================================
# 📊 Запускаю: Обновление HTML файлов
# ============================================================
# 🚀 Обновляю ВСЕ HTML файлы для использования WebP...
# ✅ index.html: заменено 120 ссылок
# ...
# ✅ Всего заменено ссылок: 120
# 
# ============================================================
# 📊 Запускаю: Удаление старых PNG файлов
# ============================================================
# 🗑️  Начинаю удаление старых PNG файлов...
# ✅ Удален: img/bottle1.png
# ✅ Удален: img/bottle2.png
# ... 150+ файлов
# ✅ Успешно удалено: 150 файлов
# 
# ✨ МИГРАЦИЯ ЗАВЕРШЕНА УСПЕШНО!
```

### Пример 2: Пошаговый Запуск

```bash
# Шаг 1: Только конвертация
npm run convert-all-webp
# Создает WebP файлы, PNG остаются

# Проверяем что все работает
ls -la img/*.webp | wc -l  # Должно быть 150+

# Шаг 2: Обновляем HTML
npm run update-html-comprehensive
# Меняет PNG на WebP в ссылках

# Проверяем HTML
grep "\.webp" index.html | wc -l  # Должно быть 120+

# Шаг 3: Удаляем PNG
npm run delete-png
# Удаляет старые PNG файлы

# Финальная проверка
ls img/*.png 2>/dev/null | wc -l  # Должно быть 0
```

### Пример 3: Частичная Миграция (Только Premium)

Если ты хочешь конвертировать только Prime продукты:

```bash
# Используй старый скрипт
npm run convert-prime-webp
npm run update-html-webp

# Новые PNG останут для Premium
```

### Пример 4: Разные Качества

Если нужно изменить качество конвертации:

```javascript
// В scripts/convert-all-to-webp.js
// Найти строку:
await sharp(fullPath)
  .webp({ quality: 70 })  // ← Измени это число
  .toFile(webpPath);

// Варианты:
// quality: 60  - очень маленкий размер, может быть видна потеря качества
// quality: 70  - отлично! (По умолчанию)
// quality: 80  - высокое качество, чуть больше размер
// quality: 90  - очень высокое, близко к оригиналу
```

---

## ❓ FAQ (Частые Вопросы)

### Q: Что если у меня в проекте есть JPEG файлы?
**A:** Они тоже конвертируются! Скрипты обрабатывают `.png`, `.jpg`, и `.jpeg`.

```javascript
// В скрипте:
if (!/\.(png|jpg|jpeg)$/i.test(file)) continue;
```

---

### Q: Можно ли откатить миграцию?
**A:** Конечно! У тебя есть три варианта:

**Вариант 1: Через Git (если коммитил перед миграцией)**
```bash
git restore img/*.webp     # Восстановит старые PNG
git restore *.html         # Восстановит старые HTML
```

**Вариант 2: Удалить WebP и восстановить PNG**
```bash
rm img/*.webp              # Удаляет WebP
git restore img/*.png      # Восстановит PNG из Git
git restore *.html         # Восстановит HTML
```

**Вариант 3: Полный откат коммита**
```bash
git log --oneline          # Найти коммит перед миграцией
git revert <commit-hash>   # Откатить все изменения
```

---

### Q: Скрипт упал на half-way, что делать?
**A:** Не паникуй! processed-images.json сохранит прогресс.

```bash
# Запусти скрипт еще раз
npm run migrate-to-webp

# Он продолжит с того же места (потому что:
# - WebP файлы уже созданы (пропустит)
# - processed-images.json уже есть (использует его)
# - Второй скрипт обновит HTML
# - Третий удалит PNG
```

---

### Q: WebP не отображаются в браузере!
**A:** Проверь браузер и ссылки:

**Проверка браузера:**
- Chrome: ✅ поддерживает WebP
- Firefox: ✅ поддерживает WebP
- Safari: ✅ поддерживает WebP (iOS 14+)
- Edge: ✅ поддерживает WebP
- IE 11: ❌ НЕ поддерживает WebP

**Если старый браузер:**
Используй fallback в HTML:
```html
<picture>
  <source srcset="image.webp" type="image/webp">
  <source srcset="image.png" type="image/png">
  <img src="image.png" alt="Image">
</picture>
```

**Если ссылка неправильная:**
```bash
# Провери ссылки в HTML
grep "\.webp" index.html | head -5

# Должны быть:
# ./img/logo.webp  ✅
# img/bottle.webp  ✅
# /img/card.webp   ✅
```

---

### Q: Как изменить quality если нужно?
**A:** Отредактируй скрипт перед запуском:

```javascript
// scripts/convert-all-to-webp.js
// Строка ~30:

await sharp(fullPath)
  .webp({ quality: 70 })  // ← Измени 70 на нужное число
  .toFile(webpPath);

// Примеры:
// quality: 50  = маленький размер, худшее качество
// quality: 70  = оптимум (РЕКОМЕНДУЕТСЯ)
// quality: 85  = высокое качество, больший размер
// quality: 100 = лучшее качество, большой размер
```

Потом запусти:
```bash
npm run convert-all-webp  # Создаст WebP с новым качеством
npm run update-html-comprehensive
npm run delete-png
```

---

### Q: Сколько памяти использует процесс?
**A:** Зависит от размера изображений:

- Маленькие PNG (< 100 KB): ~10 MB памяти
- Средние PNG (100-500 KB): ~50-100 MB памяти
- Большие PNG (> 500 KB): ~200+ MB памяти

Для твоего проекта (~150 файлов): **~100-150 MB памяти**

---

### Q: Как долго длится миграция?
**A:** Зависит от количества файлов:

- 50 файлов: ~5 секунд
- 150 файлов: ~15-30 секунд ⏱️ (твой случай)
- 500+ файлов: ~2-3 минуты

---

### Q: Что если нет processed-images.json?
**A:** Скрипт update-html-comprehensive.js обработает это:

```javascript
// Если processed-images.json нет:
// 1. Попытается прочитать его
// 2. Если не найден - переключится на режим regex
// 3. Найдет все *.png в HTML напрямую
// 4. Заменит на *.webp

// Результат: все равно сработает! ✅
```

---

### Q: Совместимость WebP с хостингом?
**A:** WebP работает везде:

| Хостинг | Поддержка | Примечание |
|---------|-----------|-----------|
| Vercel | ✅ | Отлично работает |
| Netlify | ✅ | Отлично работает |
| GitHub Pages | ✅ | Отлично работает |
| AWS S3 | ✅ | Нужно установить MIME type |
| Cloudflare | ✅ | Может автоматически конвертировать |
| Apache | ✅ | Настроить MIME type |
| Nginx | ✅ | Настроить MIME type |

**Nginx конфиг:**
```nginx
types {
  image/webp webp;
}
```

**Apache .htaccess:**
```apache
AddType image/webp .webp
AddEncoding gzip .webp
```

---

### Q: Смогу ли я добавить новые PNG после миграции?
**A:** Да! Но их нужно конвертировать вручную:

```bash
# Новый файл: img/new-image.png

# Вариант 1: Запусти полную миграцию еще раз
npm run migrate-to-webp

# Вариант 2: Конвертируй только новый файл
# Используй Sharp или инструмент онлайн:
# https://cloudconvert.com/png-to-webp
```

---

### Q: Как использовать WebP в CSS?
**A:** WebP работает в CSS как обычно:

```css
/* Фоновые изображения */
.logo {
  background-image: url('./img/logo.webp');
}

/* Или с fallback */
.background {
  background-image: url('./img/fallback.png');
  background-image: url('./img/image.webp');
}
```

---

### Q: Есть ли риск потери качества?
**A:** На качество 70% - очень мало:

- **Человеческий глаз** не видит разницу между оригиналом и WebP 70%
- **Специалисты** могут заметить в pixel-perfect дизайне
- **Размер файла** уменьшается на 60-70%

**Рекомендация:** Оставь 70% - это золотая середина! 🏆

---

### Q: Что если скрипт не найдет файлы?
**A:** Проверь пути:

```bash
# Провери что директории существуют:
ls -la img/        # Должны быть PNG файлы
ls -la img-no/     # Или здесь

# Провери что файлы доступны:
ls -la img/*.png | head -5

# Если пусто - скрипт скажет:
# ⚠️ Папка img не найдена, пропускаю...
```

---

### Q: Как отследить прогресс?
**A:** Скрипты выводят подробные логи:

```bash
# Во время работы видишь:
✅ img/logo.png → logo.webp
✅ img/bottle1.png → bottle1.webp
✅ img/bottle2.png → bottle2.webp
⏳ Обработка... (идет процесс)
✨ Успешно обработано: 150
```

---

## 🎓 Что Дальше?

1. **Запусти миграцию:**
   ```bash
   npm run migrate-to-webp
   ```

2. **Проверь результаты:**
   ```bash
   ls img/*.webp | wc -l      # Должно быть 150+
   grep "\.webp" index.html   # Должны быть ссылки
   ls img/*.png 2>/dev/null   # Должно быть пусто
   ```

3. **Закоммить изменения:**
   ```bash
   git add -A
   git commit -m "✨ Миграция PNG → WebP: -62% размера"
   ```

4. **Задеплой на production!** 🚀

---

## 📞 Что-то не работает?

1. Проверь что `sharp` установлен: `npm list sharp`
2. Посмотри логи скрипта
3. Попробуй запустить по отдельности
4. Вернись на шаг назад через Git

**Удачи!** 🎉
