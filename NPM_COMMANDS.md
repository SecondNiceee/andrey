# 🔧 Все NPM Команды

## 🚀 ГЛАВНАЯ КОМАНДА (ИСПОЛЬЗУЙ ЭТУ)

### Полная Миграция - Все 3 Этапа Вместе

```bash
npm run migrate-to-webp
```

✅ Конвертирует PNG → WebP  
✅ Обновляет HTML  
✅ Удаляет старые PNG  

**Время:** ~30 сек  
**Это все что нужно!**

---

## 🎯 ЕСЛИ ХОЧЕШЬ ПО ЭТАПАМ

### Этап 1: Конвертация PNG → WebP

```bash
npm run convert-all-webp
```

**Что делает:**
- Читает все PNG файлы из `img/` и `img-no/`
- Конвертирует в WebP (качество 70%)
- Сохраняет рядом с оригиналом
- Создает `processed-images.json`

**Результат:** 150 WebP файлов + JSON лист

---

### Этап 2: Обновление HTML Файлов

```bash
npm run update-html-comprehensive
```

**Что делает:**
- Находит все `.html` файлы
- Заменяет ссылки PNG → WebP
- Сохраняет обновленный HTML

**Результат:** HTML файлы обновлены

---

### Этап 3: Удаление Старых PNG

```bash
npm run delete-png
```

**Что делает:**
- Читает список из `processed-images.json`
- Удаляет каждый PNG файл
- Логирует результат

**Результат:** PNG удалены, осталось только WebP

---

## 📦 СТАРЫЕ КОМАНДЫ (Для Совместимости)

### Конвертация Prime Только

```bash
npm run convert-prime-webp
```

Конвертирует только Prime продукты.

---

### Обновление HTML для Prime

```bash
npm run update-html-webp
```

Обновляет HTML только для Prime.

---

### Конвертация Всех + Обновление HTML (Без Удаления)

```bash
npm run optimize-images
```

Делает:
1. `npm run convert-all-webp`
2. `npm run update-all-html-webp`

**Без удаления PNG!**

---

### Конвертация Prime + Обновление HTML для Prime

```bash
npm run optimize-prime-only
```

Для отдельной обработки Prime продуктов.

---

## 🔍 ПРОВЕРОЧНЫЕ КОМАНДЫ

### Посмотреть WebP Файлы

```bash
ls -la img/*.webp | head -10
```

Показывает первые 10 WebP файлов.

---

### Посчитать WebP Файлы

```bash
ls img/*.webp | wc -l
```

Должно быть: **150+**

---

### Посчитать PNG Файлы

```bash
ls img/*.png 2>/dev/null | wc -l
```

Должно быть: **0** (после удаления)

---

### Посмотреть ссылки на WebP в HTML

```bash
grep "\.webp" index.html | head -5
```

Должны быть ссылки вроде:
```html
<img src="./img/logo.webp" />
```

---

### Посчитать ссылки на WebP

```bash
grep -c "\.webp" index.html
```

Должно быть: **120+**

---

### Размер Директории с Изображениями

```bash
du -sh img/
```

Было: ~9 MB  
Стало: ~3.5 MB (-62%)

---

## 🔧 GIT КОМАНДЫ

### Посмотреть Что Изменилось

```bash
git status
```

Должны быть:
- Новые WebP файлы
- Обновленный index.html
- Удаленные PNG файлы

---

### Сделать Коммит

```bash
git add -A
git commit -m "✨ Миграция PNG → WebP: -62% размера"
```

---

### Откатить Все Изменения

```bash
git restore .
```

Вернет все как было.

---

### Откатить Только WebP

```bash
git restore img/*.webp
```

---

### Откатить Только HTML

```bash
git restore *.html
```

---

### Откатить Только Удаленные PNG

```bash
git restore img/*.png
```

---

## 📊 ВСЕ КОМАНДЫ В ОДНОМ МЕСТЕ

### npm run (основные)

```bash
npm run migrate-to-webp                 # ВСЕ ВМЕСТЕ (главная!)
npm run convert-all-webp                # Только конвертация
npm run update-html-comprehensive       # Только обновление HTML
npm run delete-png                      # Только удаление PNG
```

### npm run (старые, для совместимости)

```bash
npm run convert-prime-webp              # Prime только
npm run update-html-webp                # Prime HTML
npm run convert-all-webp                # Все PNG
npm run update-all-html-webp            # Все HTML (старая версия)
npm run optimize-images                 # Конвертация + обновление
npm run optimize-prime-only             # Prime только (оба этапа)
```

### Проверка (ls, grep, du)

```bash
ls img/*.webp | wc -l                   # Есть ли WebP
ls img/*.png | wc -l                    # Остались ли PNG
grep -c "\.webp" index.html             # WebP ссылки в HTML
du -sh img/                             # Размер папки
```

### Git

```bash
git status                              # Что изменилось
git add -A                              # Добавить все
git commit -m "Message"                 # Коммит
git push                                # Пушь на сервер
git restore .                           # Откатить все
```

---

## 🎯 СЦЕНАРИИ

### Сценарий 1: Полная Миграция

```bash
# Запусти все
npm run migrate-to-webp

# Проверь результаты
ls img/*.webp | wc -l    # 150+
ls img/*.png | wc -l     # 0
grep -c "\.webp" index.html  # 120+

# Сохрани в Git
git add -A
git commit -m "✨ PNG → WebP миграция"
git push
```

---

### Сценарий 2: Пошаговая Миграция

```bash
# Шаг 1: Конвертация
npm run convert-all-webp

# Проверка
ls img/*.webp | wc -l    # 150+

# Шаг 2: Обновление HTML
npm run update-html-comprehensive

# Проверка
grep -c "\.webp" index.html  # 120+

# Шаг 3: Удаление PNG
npm run delete-png

# Проверка
ls img/*.png | wc -l     # 0

# Git
git add -A
git commit -m "✨ PNG → WebP миграция"
```

---

### Сценарий 3: Откат Все

```bash
# Если что-то не так
git restore .

# Все вернется как было!
```

---

### Сценарий 4: Откат Только WebP

```bash
# Хочешь оставить PNG и HTML, удалить WebP
git restore img/*.webp

# Теперь в img/ только PNG
ls img/ | grep webp | wc -l  # 0
```

---

## 📋 БЫСТРАЯ СПРАВКА

| Что Нужно | Команда |
|-----------|---------|
| Полная миграция | `npm run migrate-to-webp` |
| Только конвертация | `npm run convert-all-webp` |
| Только обновление HTML | `npm run update-html-comprehensive` |
| Только удаление PNG | `npm run delete-png` |
| Проверить WebP | `ls img/*.webp \| wc -l` |
| Проверить PNG | `ls img/*.png \| wc -l` |
| WebP ссылки в HTML | `grep -c "\.webp" index.html` |
| Откатить все | `git restore .` |

---

## 🎓 ОБЪЯСНЕНИЯ

### Что Такое `npm run`?

`npm run` запускает скрипты из `package.json`.

```json
{
  "scripts": {
    "migrate-to-webp": "node scripts/full-migration-to-webp.js"
  }
}
```

Когда ты пишешь:
```bash
npm run migrate-to-webp
```

npm запускает файл: `scripts/full-migration-to-webp.js`

---

### Что Такое `ls`?

`ls` - команда для просмотра файлов в папке.

```bash
ls img/*.webp        # Все WebP в img/
ls img/*.png         # Все PNG в img/
ls -la img/          # Все файлы с деталями
```

---

### Что Такое `grep`?

`grep` - команда для поиска текста.

```bash
grep "\.webp" index.html     # Ищет ".webp" в HTML
grep -c "\.webp" index.html  # Считает сколько есть
grep -c "\.png" index.html   # Считает PNG
```

---

### Что Такое `du`?

`du` - команда для проверки размера папки.

```bash
du -sh img/   # Размер папки img/
```

---

### Что Такое `git restore`?

`git restore` откатывает изменения из Git.

```bash
git restore .              # Откатить все
git restore img/*.webp     # Откатить только WebP
git restore *.html         # Откатить только HTML
```

---

## ⚠️ ВАЖНЫЕ ЗАМЕЧАНИЯ

### Замечание 1: Первый Запуск

Убедись что в папке есть `processed-images.json` после конвертации. Это файл со списком обработанных PNG.

### Замечание 2: Откатить PNG

Если ты удалил PNG случайно:

```bash
git restore img/*.png   # Восстановит из Git
```

### Замечание 3: Node.js Должен Быть Установлен

```bash
node --version    # Должно быть число (например 18.0.0)
npm --version     # Должно быть число (например 9.0.0)
```

### Замечание 4: Sharp Должен Быть Установлен

```bash
npm list sharp    # Должно быть написано sharp@0.32.0
```

Если нет:
```bash
npm install sharp
```

---

## 💡 СОВЕТЫ

### Совет 1: Используй Главную Команду

```bash
npm run migrate-to-webp    # Просто и работает!
```

Не нужно запускать по отдельности.

### Совет 2: Проверяй Результаты

После каждого шага проверяй:

```bash
ls img/*.webp | wc -l   # WebP есть?
grep -c "\.webp" index.html  # HTML обновлены?
```

### Совет 3: Используй Git

Git позволяет откатить что угодно:

```bash
git restore .   # Все вернется!
```

### Совет 4: Читай Логи

Скрипты выводят подробные логи. Смотри на них!

```
✅ img/logo.png → logo.webp
✅ img/bottle1.png → bottle1.webp
...
```

---

## 🚀 НАЧНИ ПРЯМО СЕЙЧАС

```bash
npm run migrate-to-webp
```

**Это всё!** Жди ~30 секунд и готово! 🎉

---

**Удачи!** 💪
