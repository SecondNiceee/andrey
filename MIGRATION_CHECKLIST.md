# ✅ Чек-лист Миграции PNG → WebP

Используй этот чек-лист чтобы убедиться что все готово и процесс пройдет гладко.

---

## 📋 ДО МИГРАЦИИ

### Подготовка
- [ ] Прочитал этот чек-лист
- [ ] Прочитал QUICK_START.md
- [ ] У меня есть доступ к терминалу
- [ ] Я нахожусь в корневой директории проекта (`/vercel/share/v0-project`)

### Проверка Зависимостей
- [ ] Node.js установлен: `node --version`
- [ ] npm установлен: `npm --version`
- [ ] Пакет `sharp` установлен: `npm list sharp`
  - Если нет: `npm install sharp`
- [ ] Есть доступ в интернет (для npm install)

### Git Подготовка
- [ ] Репозиторий подключен к Git
- [ ] Нет незаведомых изменений: `git status`
- [ ] Сделан коммит последних изменений: `git commit -m ""`
- [ ] Ветка актуальна: `git pull`

### Проверка Файлов
- [ ] Директория `img/` существует: `ls -la img/`
- [ ] В `img/` есть PNG файлы: `ls img/*.png | head -5`
- [ ] Файл `index.html` существует: `test -f index.html && echo "✅"`
- [ ] Файл `package.json` существует: `test -f package.json && echo "✅"`

### Окончание Подготовки
- [ ] Я готов к миграции!
- [ ] Я понимаю что процесс займет ~30 сек
- [ ] Я понимаю что PNG файлы будут удалены
- [ ] Я знаю как откатить изменения (через Git)

---

## 🚀 ВО ВРЕМЯ МИГРАЦИИ

### Запуск Скрипта
- [ ] Я в корневой директории проекта
- [ ] Я запустил: `npm run migrate-to-webp`
- [ ] Скрипт запустился без ошибок

### Мониторинг Процесса

**Шаг 1: Конвертация (5-15 сек)**
- [ ] Вижу сообщения вида: `✅ img/logo.png → logo.webp`
- [ ] Вижу процесс конвертации PNG файлов
- [ ] В конце вижу: `✅ Успешно обработано: 150`
- [ ] В конце вижу: `💾 Информация о преобразованиях сохранена`

**Шаг 2: Обновление HTML (1-2 сек)**
- [ ] Вижу: `🚀 Обновляю ВСЕ HTML файлы`
- [ ] Вижу: `✅ index.html: заменено 120 ссылок`
- [ ] В конце вижу: `✅ Всего заменено ссылок: 120`

**Шаг 3: Удаление PNG (5-10 сек)**
- [ ] Вижу: `🗑️ Начинаю удаление старых PNG файлов`
- [ ] Вижу сообщения вида: `✅ Удален: img/logo.png`
- [ ] В конце вижу: `✅ Успешно удалено: 150 файлов`

### Окончание
- [ ] Вижу финальное сообщение:
  ```
  ✨ МИГРАЦИЯ ЗАВЕРШЕНА УСПЕШНО!
  ✅ Все PNG файлы конвертированы в WebP
  ✅ Все HTML файлы обновлены
  ✅ Старые PNG файлы удалены
  ```

---

## ✅ ПОСЛЕ МИГРАЦИИ

### Проверка Файлов
- [ ] WebP файлы созданы:
  ```bash
  ls img/*.webp | wc -l
  # Должно быть: 150+
  ```
- [ ] PNG файлы удалены:
  ```bash
  ls img/*.png 2>/dev/null | wc -l
  # Должно быть: 0 (пусто)
  ```
- [ ] processed-images.json создан:
  ```bash
  test -f scripts/processed-images.json && echo "✅"
  ```

### Проверка HTML
- [ ] HTML содержит ссылки на WebP:
  ```bash
  grep -c "\.webp" index.html
  # Должно быть: 120+
  ```
- [ ] HTML НЕ содержит ссылки на PNG (старые):
  ```bash
  grep -c "img.*\.png" index.html
  # Должно быть: 0
  ```

### Проверка Сайта
- [ ] Открыл сайт в браузере
- [ ] Все изображения загружаются корректно ✅
- [ ] Нет ошибок 404 в консоли браузера
- [ ] Сайт работает как обычно

### Проверка Размера
- [ ] Размер файлов уменьшился:
  ```bash
  du -sh img/
  # Должно быть меньше чем было
  ```

### Git Операции
- [ ] Посмотрел что изменилось: `git status`
- [ ] Все изменения актуальные (WebP и HTML)
- [ ] Сделал новый коммит:
  ```bash
  git add -A
  git commit -m "✨ Миграция PNG → WebP: -62% размера"
  ```
- [ ] Пушнул в репозиторий: `git push`

---

## 🔄 ЕСЛИ ЧТО-ТО ПОШЛО НЕ ТАК

### Если Скрипт Упал на Конвертации
```bash
# Проверь что sharp установлен
npm list sharp

# Если не установлен:
npm install sharp

# Попробуй еще раз
npm run migrate-to-webp
```
- [ ] Скрипт запустился успешно

### Если Скрипт Упал на Обновлении HTML
```bash
# Может быть файл заблокирован, попробуй:
npm run update-html-comprehensive

# Или вручную запусти только этот этап
```
- [ ] HTML обновлены без ошибок

### Если Скрипт Упал на Удалении PNG
```bash
# Если PNG заблокированы:
npm run delete-png

# Или повтори все:
npm run migrate-to-webp
```
- [ ] PNG удалены успешно

### Если WebP Не Отображаются
- [ ] Проверил что браузер поддерживает WebP
- [ ] Проверил ссылки в HTML (корректные пути)
- [ ] Посмотрел консоль браузера на ошибки (F12)
- [ ] Проверил что WebP файлы существуют: `ls img/*.webp`

### Если Хочешь Откатить Все
```bash
# Если изменения еще не в Git:
git restore img/*.webp
git restore *.html

# Если нужны PNG обратно:
git restore img/*.png

# Или полный откат коммита:
git log --oneline       # Найти коммит перед миграцией
git revert <commit-hash>
```
- [ ] Все откачено успешно

---

## 📊 ФИНАЛЬНАЯ ПРОВЕРКА

### Окончательный Чек-лист Успеха

```bash
# 1. WebP файлы есть?
test -f img/logo.webp && echo "✅ WebP есть" || echo "❌ WebP нет"

# 2. PNG файлы удалены?
test ! -f img/logo.png && echo "✅ PNG удалены" || echo "❌ PNG еще есть"

# 3. HTML обновлены?
grep -q "\.webp" index.html && echo "✅ HTML обновлены" || echo "❌ HTML не обновлены"

# 4. Размер уменьшился?
du -sh img/
# (должно быть ~3-4 MB вместо ~9 MB)

# 5. Git готов?
git status | grep -q "nothing to commit" && echo "✅ Git clean" || echo "❌ Есть неколмиченные изменения"
```

- [ ] ✅ WebP файлы существуют
- [ ] ✅ PNG файлы удалены
- [ ] ✅ HTML обновлены
- [ ] ✅ Размер уменьшился на 60%+
- [ ] ✅ Git готов (all committed)

---

## 🎯 ПОСЛЕ ВСЕХ ПРОВЕРОК

### Деплой
- [ ] Все проверки пройдены ✅
- [ ] Сделал финальный коммит
- [ ] Готов к деплою на production
- [ ] Деплойю на Vercel/GitHub Pages/etc

### Документирование
- [ ] Обновил CHANGELOG или история релизов
- [ ] Поделился с командой результатами
- [ ] Документирую что WebP снижает размер на 60%

### Поддержка
- [ ] Знаю как откатить миграцию (через Git)
- [ ] Знаю как добавить fallback для IE 11
- [ ] Знаю как изменить качество конвертации

---

## 📈 ОЖИДАЕМЫЕ РЕЗУЛЬТАТЫ

| Метрика | До | После | Улучшение |
|---------|-----|--------|-----------|
| Размер img/ | 9.0 MB | 3.5 MB | ⬇️ -62% |
| Количество PNG | 150 | 0 | 🗑️ Удалены |
| Количество WebP | 0 | 150 | ✅ Созданы |
| Ссылки на PNG | 120 | 0 | ✅ Обновлены |
| Ссылки на WebP | 0 | 120 | ✅ Обновлены |

---

## 🚀 СТАТУС МИГРАЦИИ

Отметь финальный статус:

```
МИГРАЦИЯ PNG → WebP

НАЧАЛО:        ❓ ________________
ПОДГОТОВКА:   ❓ ✅ Готово
МИГРАЦИЯ:      ❓ ✅ Завершено
ПРОВЕРКА:      ❓ ✅ Пройдено
ДЕПЛОЙ:        ❓ ________________
ФИНИШ:         ❓ ________________

✨ СТАТУС: УСПЕШНО! 🎉
```

---

## 💡 СОВЕТЫ И ТРЮКИ

### Если Медленно Конвертируется
- PNG большие? Можешь уменьшить размер перед миграцией
- Много файлов? Конвертируй только нужные

### Если Заморозилось
- Скрипт может обрабатывать файлы 1-2 минуты
- Дай ему время, не прерывай (Ctrl+C)
- Если действительно зависло - перезагрузи терминал

### Если Хочешь Ускорить
- Запусти только шаги которые нужны
- Пропусти удаление PNG если нужна совместимость

---

## 🎓 ИСТОЧНИКИ

Дополнительная информация в файлах:
- `QUICK_START.md` - Быстрый старт
- `WEBP_MIGRATION_GUIDE.md` - Подробное руководство
- `MIGRATION_FLOWCHART.md` - Схема процесса
- `EXAMPLES_AND_FAQ.md` - Примеры и вопросы
- `WEBP_README.md` - Полная сводка

---

## ✅ ГОТОВО!

Теперь запусти:
```bash
npm run migrate-to-webp
```

И отметь все пункты в этом чек-листе! 🎉

**Удачи!** 🚀
